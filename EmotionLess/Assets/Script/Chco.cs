using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chco : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("choco and bite");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
